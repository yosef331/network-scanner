#  SCANNET-PRO 🌐

![Banner](https://raw.githubusercontent.com/yosef331/network-scanner/main/docs/banner.png )

أداة فحص شبكات احترافية مكتوبة بلغة بايثون، مصممة لتكون جزءاً من مشروع مادة "البرمجة للأمن السيبراني" تحت إشراف الدكتور عادل فيصل معوضه.

---

## 📜 الفكرة العامة

SCANNET-PRO هي أداة تعمل من خلال سطر الأوامر (CLI) تمكن مختبري الاختراق ومحللي الشبكات من:
- **اكتشاف الأجهزة النشطة** على أي شبكة محلية.
- **فحص المنافذ المفتوحة** على الأجهزة المكتشفة لمعرفة الخدمات التي تعمل عليها.
- **حفظ نتائج الفحص** في ملف `CSV` لتحليلها لاحقاً أو لتوثيقها في التقارير.

تم بناء الأداة مع التركيز على الاحترافية، تنظيم الكود، ومعالجة الأخطاء.

---

## ✨ الميزات الرئيسية

- **اكتشاف الأجهزة:** استخدام بروتوكول ARP لاكتشاف جميع الأجهزة المتصلة بالشبكة بسرعة وكفاءة.
- **فحص المنافذ:** فحص المنافذ المفتوحة باستخدام تقنيات `TCP Connect` السريعة.
- **واجهة CLI سهلة:** واجهة سطر أوامر تفاعلية ومنظمة باستخدام مكتبة `argparse`.
- **تخصيص الفحص:** إمكانية تحديد نطاق الشبكة وقائمة المنافذ المراد فحصها.
- **واجهة بصرية جذابة:** استخدام الألوان والبانرات لتحسين تجربة المستخدم.
- **تصدير النتائج:** حفظ النتائج بصيغة `CSV` سهلة القراءة والمعالجة.
- **معالجة الأخطاء:** التعامل الذكي مع الأخطاء الشائعة مثل عدم وجود صلاحيات أو إدخال صيغ غير صحيحة.

---

## 🛠️ التقنيات المستخدمة

- **اللغة:** Python 3
- **المكتبات الأساسية:**
  - `Scapy`: لإرسال حزم ARP واكتشاف الأجهزة.
  - `Socket`: لإجراء اتصالات TCP وفحص المنافذ.
  - `Argparse`: لبناء واجهة سطر الأوامر (CLI).
  - `PyFiglet`: لإنشاء البانر الفني.
  - `Termcolor`: لتلوين المخرجات وتحسين الواجهة.

---

## 🚀 كيفية الاستخدام

### 1. المتطلبات

تأكد من تثبيت المكتبات اللازمة. يمكنك تثبيتها باستخدام `apt` على أنظمة Debian/Kali:
```bash
sudo apt update
sudo apt install python3-scapy python3-pyfiglet python3-termcolor
```

### 2. استنساخ المستودع

```bash
git clone https://github.com/yosef331/network-scanner.git
cd network-scanner
```

### 3. أمثلة على الأوامر

**تنبيه:** يجب تشغيل الأداة بصلاحيات المدير (`sudo` ) لتتمكن من الوصول إلى كرت الشبكة.

- **فحص بسيط لاكتشاف الأجهزة فقط:**
  ```bash
  sudo python3 main.py -t 192.168.1.1/24
  ```

- **فحص الأجهزة مع تفعيل فحص المنافذ (باستخدام القائمة الافتراضية):**
  ```bash
  sudo python3 main.py -t 192.168.1.1/24 --scan-ports
  ```

- **فحص المنافذ المحددة وحفظ النتائج في ملف:**
  ```bash
  sudo python3 main.py -t 192.168.1.1/24 --scan-ports --ports 22,80,443 -o results.csv
  ```

---

## 👥 فريق العمل

- **يوسف (قائد الفريق والمطور الرئيسي):**
  - تصميم بنية المشروع وهيكله.
  - كتابة الكود الأساسي لوظائف فحص الشبكة وفحص المنافذ.
  - بناء وتصميم واجهة سطر الأوامر (CLI).
  - الإشراف على الاختبار وتصحيح الأخطاء.
  - إعداد التوثيق النهائي للمشروع.



---

## 👨‍🏫 إشراف

**الدكتور/ عادل فيصل معوضه**
