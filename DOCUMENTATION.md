# 🚀 مشروع SCANNET-PRO: تقرير فني شامل

---

## 📝 **مقدمة**

هذا المستند يقدم شرحاً فنياً كاملاً لأداة **SCANNET-PRO**، التي تم تطويرها كمشروع نهائي لمادة "البرمجة للأمن السيبراني". الأداة عبارة عن فاحص شبكات متكامل يعمل من سطر الأوامر، تم بناؤه باستخدام لغة بايثون مع التركيز على الكفاءة، التنظيم، والاحترافية.

- **اسم المشروع:** SCANNET-PRO
- **إشراف الدكتور:** د. عادل فيصل معوضه
- **إعداد الطالب:** يوسف حسين الأحرمي (مطور منفرد)

---

## 🎯 **الفكرة العامة والأهداف**

الهدف من المشروع هو بناء أداة أمن سيبراني عملية وفعالة تحاكي الأدوات المستخدمة في العالم الحقيقي. تم اختيار فكرة "فاحص الشبكات" لأهميتها كخطوة أولى في أي عملية اختبار اختراق، حيث توفر معلومات استخباراتية أساسية عن الشبكة المستهدفة.

**الأهداف التي تم تحقيقها:**
1.  **اكتشاف الأجهزة:** تحديد جميع الأجهزة النشطة على الشبكة المحلية.
2.  **فحص الخدمات:** معرفة المنافذ المفتوحة (الخدمات) على كل جهاز.
3.  **واجهة احترافية:** بناء واجهة سطر أوامر (CLI) سهلة الاستخدام.
4.  **توثيق النتائج:** القدرة على حفظ المخرجات في ملف لتحليلها لاحقاً.
5.  **كود عالي الجودة:** تطبيق مفاهيم البرمجة النظيفة، معالجة الأخطاء، وتنظيم الكود.

---

## 🏛️ **هيكلية المشروع**

تم تصميم المشروع بهيكلية معيارية (Modular) لفصل كل جزء من المنطق في ملف خاص به، مما يسهل الصيانة والتطوير.

```
network_scanner/
├── docs/                  # (مجلد مستقبلي للتوثيقات الإضافية)
├── scanner/
│   ├── __init__.py      # يخبر بايثون أن هذا المجلد هو "باكج"
│   ├── core.py          # 🔥 قلب الأداة: يحتوي على منطق الفحص
│   └── cli.py           # 💻 واجهة الأداة: يتعامل مع المستخدم
├── main.py              # 🏁 نقطة انطلاق البرنامج
├── .gitignore           # يخبر Git بتجاهل الملفات المؤقتة
└── README.md            # الواجهة التعريفية للمشروع على GitHub
```

---

## 💻 **شرح الأكواد بالتفصيل**

### **1. ملف `main.py` (نقطة الانطلاق)**

هذا الملف هو أبسط ملف في المشروع، ووظيفته الوحيدة هي استدعاء الواجهة الرئيسية للأداة.

```python
#!/usr/bin/env python3
# main.py

# 1. نستورد دالة main من ملف الواجهة (cli.py)
from scanner.cli import main

# 2. نتأكد أن هذا الملف هو الذي يتم تشغيله مباشرة
if __name__ == "__main__":
    # 3. إذا كان كذلك، نقوم بتشغيل الدالة main التي استوردناها
    main()
```
**ببساطة:** عند تنفيذ `python3 main.py`، يتم تشغيل الكود الموجود في `scanner/cli.py`.

### **2. ملف `scanner/core.py` (القلب النابض للأداة)**

هذا الملف يحتوي على كل المنطق الثقيل والوظائف الأساسية.

```python
# scanner/core.py

# --- 1. استيراد المكتبات ---
import os      # للتحقق من صلاحيات المستخدم (هل هو root؟)
import csv     # للتعامل مع ملفات CSV (حفظ النتائج)
import socket  # لفحص المنافذ عبر اتصالات TCP

# نحاول استيراد scapy، وإذا لم تكن موجودة، نبلغ المستخدم
try:
    from scapy.all import ARP, Ether, srp
except ImportError:
    print("خطأ: مكتبة scapy غير مثبتة.")
    exit()

# --- 2. دالة فحص الشبكة (scan_network) ---
def scan_network(target_ip, print_fn):
    # نتحقق من هوية المستخدم. 0 يعني المستخدم root (المدير)
    if os.geteuid() != 0:
        print_fn("\n[!] خطأ: هذه الأداة تتطلب صلاحيات المدير.", 'red')
        return None # نرجع "لا شيء" لإيقاف العملية

    print_fn(f"[*] جاري فحص الشبكة: {target_ip}", 'blue')

    # أ. إنشاء حزمة ARP: تسأل "من يملك عنوان IP هذا؟"
    arp_request = ARP(pdst=target_ip)
    # ب. إنشاء إطار إيثرنت: يحدد أن الرسالة يجب أن تُرسل للجميع (broadcast)
    broadcast = Ether(dst="ff:ff:ff:ff:ff:ff")
    # ج. دمج الحزمتين: نضع حزمة ARP داخل إطار الإيثرنت
    arp_request_broadcast = broadcast / arp_request

    # د. إرسال الحزمة واستقبال الردود (srp = Send and Receive Packet)
    # timeout=3: انتظر 3 ثوانٍ فقط للردود
    answered_list = srp(arp_request_broadcast, timeout=3, verbose=0)

    # هـ. معالجة الردود وتجهيزها في قائمة مرتبة
    clients_list = []
    for element in answered_list:
        # element يحتوي على الحزمة المستقبلة (الرد)
        client_dict = {"ip": element.psrc, "mac": element.hwsrc}
        clients_list.append(client_dict)

    if clients_list:
        print_fn(f"[+] تم العثور على {len(clients_list)} جهاز.", 'green')
    
    return clients_list

# --- 3. دالة فحص المنافذ (port_scan) ---
def port_scan(clients, ports, print_fn):
    # نمر على كل جهاز تم اكتشافه
    for i, client in enumerate(clients):
        ip = client['ip']
        client['open_ports'] = [] # ننشئ قائمة فارغة لتخزين المنافذ المفتوحة

        print_fn(f"    -> جاري فحص المنافذ للجهاز {ip} ({i+1}/{len(clients)})", 'blue')

        # نمر على كل منفذ مطلوب فحصه
        for port in ports:
            try:
                # أ. ننشئ "مقبس" (socket) جديد لكل محاولة اتصال
                sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
                # ب. نضبط مهلة زمنية قصيرة لتسريع الفحص
                socket.setdefaulttimeout(0.5)
                # ج. نحاول الاتصال. connect_ex ترجع 0 إذا نجح الاتصال
                result = sock.connect_ex((ip, port))
                if result == 0:
                    # د. إذا نجح، نضيف المنفذ إلى قائمة المنافذ المفتوحة
                    client['open_ports'].append(port)
                # هـ. نغلق المقبس استعداداً للمحاولة التالية
                sock.close()
            except socket.error as e:
                print_fn(f"    [!] خطأ في الاتصال بـ {ip}:{port} - {e}", 'red')

# --- 4. دالة حفظ النتائج (save_to_csv) ---
def save_to_csv(clients, filename, print_fn):
    try:
        # نفتح ملفاً جديداً في وضع الكتابة ('w')
        with open(filename, 'w', newline='') as csvfile:
            # نحدد أسماء الأعمدة
            fieldnames = ['ip', 'mac', 'open_ports']
            writer = csv.DictWriter(csvfile, fieldnames=fieldnames)

            # نكتب السطر الأول (أسماء الأعمدة)
            writer.writeheader()
            # نمر على كل جهاز ونكتب بياناته في سطر جديد
            for client in clients:
                client['open_ports'] = ",".join(map(str, client.get('open_ports', [])))
                writer.writerow(client)
        print_fn(f"\n[+] تم حفظ النتائج بنجاح في الملف: {filename}", 'green')
    except IOError:
        print_fn(f"\n[!] خطأ: لم أتمكن من الكتابة إلى الملف {filename}.", 'red')
```

### **3. ملف `scanner/cli.py` (الواجهة الأمامية)**

هذا الملف هو الوسيط بين المستخدم والأداة. وظيفته هي أخذ الأوامر من المستخدم، التحقق منها، ثم استدعاء الوظائف المناسبة من `core.py`.

```python
# scanner/cli.py

# --- 1. استيراد المكتبات ---
import argparse  # لبناء واجهة سطر الأوامر
import ipaddress # للتحقق من صحة صيغة الشبكة
import socket    # للحصول على عنوان IP المحلي
# استيراد الدوال من ملف core.py
from .core import scan_network, save_to_csv, port_scan
# استيراد مكتبات الواجهة البصرية
from pyfiglet import Figlet
from termcolor import colored

# --- 2. دالة main (الدالة الرئيسية للواجهة) ---
def main():
    # أ. طباعة البانر الفني
    f = Figlet(font='slant')
    banner = f.renderText('Net Scanner')
    print(colored(banner, 'cyan'))

    # ب. إعداد argparse لوصف الأداة وتحديد الخيارات
    parser = argparse.ArgumentParser(...)
    
    # ج. إضافة الخيارات التي يمكن للمستخدم إدخالها
    # خيار -t أو --target لتحديد الشبكة (إجباري)
    network_group.add_argument("-t", "--target", ...)
    # خيار -o أو --output لحفظ الملف (اختياري)
    network_group.add_argument("-o", "--output", ...)
    # خيار --scan-ports لتفعيل فحص المنافذ (مفتاح تشغيل/إيقاف)
    port_group.add_argument("--scan-ports", action="store_true", ...)
    # خيار --ports لتحديد قائمة المنافذ (له قيمة افتراضية)
    port_group.add_argument("--ports", ...)

    # د. تحليل الأوامر التي أدخلها المستخدم
    options = parser.parse_args()

    # هـ. التحقق من صحة المدخلات (صيغة الشبكة)
    try:
        ipaddress.ip_network(options.target_ip, strict=False)
    except ValueError:
        print(colored("\n[!] خطأ: صيغة الشبكة غير صالحة.", 'red'))
        return

    # و. تعريف دالة مساعدة للطباعة الملونة
    def print_status(message, color='blue'):
        print(colored(message, color))
    
    # --- 3. تنفيذ منطق الأداة ---
    
    # أ. استدعاء دالة فحص الشبكة
    scan_result = scan_network(options.target_ip, print_fn=print_status)
    
    # ب. إضافة الجهاز المحلي إلى قائمة الفحص (تحسين مهم)
    if options.scan_ports:
        # ... (كود الحصول على IP المحلي وإضافته للقائمة) ...

    # ج. التحقق إذا طلب المستخدم فحص المنافذ
    if options.scan_ports:
        # ... (كود تحويل نص المنافذ إلى قائمة أرقام) ...
        # استدعاء دالة فحص المنافذ
        port_scan(scan_result, ports, print_fn=print_status)

    # د. طباعة النتائج النهائية في جدول منظم وملون
    # ... (كود طباعة الجدول) ...

    # هـ. التحقق إذا طلب المستخدم حفظ النتائج
    if options.output_file:
        # استدعاء دالة حفظ النتائج
        save_to_csv(scan_result, options.output_file, print_fn=print_status)
```

---

## ✅ **الخاتمة**

تم بحمد الله إنجاز هذا المشروع بنجاح، حيث تم تحقيق جميع الأهداف المطلوبة. لقد كانت رحلة تعليمية ممتعة تم من خلالها تطبيق العديد من المفاهيم البرمجية والأمنية بشكل عملي. أصبحت أداة SCANNET-PRO الآن أداة قوية ومتكاملة، جاهزة للاستخدام والتطوير المستقبلي.

